<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:task="http://www.springframework.org/schema/task"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd
       http://www.springframework.org/schema/task
       http://www.springframework.org/schema/task/spring-task.xsd">

  <context:component-scan base-package="com.youzan.trade"/>
  <context:annotation-config/>

  <task:annotation-driven executor="orderDeliveredThreadPoolTaskExecutor"
                          scheduler="threadPoolTaskScheduler"
                          mode="proxy"/>
  <task:scheduler id="threadPoolTaskScheduler" pool-size="10"/>
  <!-- 处理订单已发货的延时任务的线程池的配置 -->
  <task:executor id="orderDeliveredThreadPoolTaskExecutor" pool-size="16-32" queue-capacity="5000"
                 keep-alive="240"
                 rejection-policy="CALLER_RUNS"/>
  <!-- 处理维权的延时任务的线程池的配置 -->
  <task:executor id="safeThreadPoolTaskExecutor" pool-size="4-8" queue-capacity="3000"
                 keep-alive="60"
                 rejection-policy="CALLER_RUNS"/>

  <bean id="propertyPlaceholderConfigurer"
        class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
    <property name="ignoreResourceNotFound" value="true"/>
    <property name="locations">
      <list>
        <value>file:${props.path}/conf.properties</value>
        <value>file:${props.path}/jdbc.properties</value>
      </list>
    </property>
  </bean>

  <import resource="classpath:dal/persistence.xml"/>
  <import resource="classpath:biz/*/beans-*.xml"/>
</beans>


